version: 3
projects:
- workflow: my-workflow
  name: my-project-name
  dir: .
workflows:
  my-workflow:
    plan:
      steps:
      - init
      - plan
      - run: "terraform show -no-color -json $PLANFILE"